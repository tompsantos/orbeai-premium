# contracts

Contratos versionados entre `web`, `control-api` e `cognition`.

O primeiro contrato disponível é:

```http
POST /v1/turns
X-Orbe-Internal-Key: ***
```

Campos mínimos: `workspace_id`, `user_id`, `chat_id` e `message`.
