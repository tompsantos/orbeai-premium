/**
 * orbeAI — Server projects adapter (stub).
 * Em Fase 1: CRUD real com RLS por workspace.
 */
export async function listProjectsForUser(_userId: string) {
  return { ok: false, error: "Projects server-side não implementado em mock mode." };
}
